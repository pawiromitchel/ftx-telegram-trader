const TELEGRAM_BOT_TOKEN = '';
let API_KEYS = [];
// this setting will increase the order size by 5x
let DEGEN = false;
// order size in percentage of the wallet balance
let ORDER_SIZE = 100;

module.exports = { ORDER_SIZE, TELEGRAM_BOT_TOKEN, DEGEN, API_KEYS };